import {Document} from "mongoose";

export interface ICastItem extends Document {
    cast_id: number;
    character: string;
    credit_id: string;
    gender: number;
    id: number;
    name: string;
    order: number;
    profile_path: string;
}
